<Window x:Class="IndiLogs_3._0.Views.ColumnManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Manage Columns"
        Height="400" Width="350"
        Background="{DynamicResource BgDark}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select columns to display:"
                   Foreground="{DynamicResource TextPrimary}"
                   FontSize="14" FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="{DynamicResource BgCard}"
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="1" CornerRadius="4" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ColumnsListBox" ItemsSource="{Binding Columns}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                      Content="{Binding Header}"
                                      Foreground="{DynamicResource TextPrimary}"
                                      Margin="0,5"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Reset to Default" Click="ResetButton_Click"
                    Style="{DynamicResource ModernBtn}"
                    Background="Transparent"
                    Foreground="{DynamicResource TextSecondary}"
                    Padding="12,6" Margin="0,0,10,0"/>
            <Button Content="Apply" Click="ApplyButton_Click"
                    Style="{DynamicResource ModernBtn}"
                    Background="{DynamicResource PrimaryColor}"
                    Foreground="White"
                    Padding="20,6" Margin="0,0,5,0"/>
            <Button Content="Cancel" Click="CancelButton_Click"
                    Style="{DynamicResource ModernBtn}"
                    Background="Transparent"
                    Foreground="{DynamicResource TextSecondary}"
                    Padding="20,6"/>
        </StackPanel>
    </Grid>
</Window>
