<UserControl x:Class="IndiLogs_3._0.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:IndiLogs_3._0.Converters"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="1100">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Border Background="{DynamicResource BgPanel}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.15" ShadowDepth="2" BlurRadius="6"/>
        </Border.Effect>
        <Grid Margin="15,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Section: Logo & Open Button -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                    <Button Command="{Binding OpenSettingsCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}" Style="{DynamicResource ModernBtn}" Width="28" Height="28" ToolTip="Settings" Padding="0" Background="Transparent" BorderThickness="0">
                        <TextBlock Text="⚙" FontFamily="Segoe UI Symbol" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                    </Button>
                    <StackPanel VerticalAlignment="Center" Margin="6,0,0,0">
                        <TextBlock Text="INDILOGS" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="VERSION 3.0" FontWeight="Normal" FontSize="8" Foreground="{DynamicResource PrimaryColor}" HorizontalAlignment="Right" Margin="0,-2,0,0"/>
                    </StackPanel>
                </StackPanel>
                <Button Command="{Binding LoadCommand}" Style="{DynamicResource PrimaryBtn}" ToolTip="Load Files... (Drag &amp; Drop supported)" Width="100" Height="28" Padding="8,4">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📂" FontSize="12" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="OPEN" FontWeight="Bold" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Center Section: Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Background="{DynamicResource BgCard}" CornerRadius="12" Padding="2" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding MarkRowCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Mark Row (Space)" Width="28" Height="22" Padding="0">
                            <TextBlock Text="📌" FontSize="11" Foreground="{DynamicResource TextPrimary}"/>
                        </Button>
                        <Button Content="⬇" Command="{Binding NextMarkedCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" ToolTip="Next Marked" Width="28" Height="22" Padding="0" FontSize="10"/>
                        <Button Content="⬆" Command="{Binding PrevMarkedCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" Foreground="{DynamicResource TextPrimary}" ToolTip="Previous Marked" Width="28" Height="22" Padding="0" FontSize="10"/>

                        <Button Command="{Binding ToggleTimeSyncCommand}" ToolTip="Toggle Time-Sync Scrolling" Width="28" Height="22" Padding="0" x:Name="TimeSyncButton">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernBtn}">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTimeSyncEnabled}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="🔗" FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsTimeSyncEnabled}" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button>

                        <Button Command="{Binding ToggleAllAnnotationsCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Show/Hide All Annotations" Width="28" Height="22" Padding="0">
                            <TextBlock Text="💬" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                        </Button>

                        <Rectangle Width="1" Fill="{DynamicResource BorderColor}" Margin="2,3"/>

                        <Button Command="{Binding ToggleSearchCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Search (Ctrl+F)" Width="28" Height="22" Padding="0">
                            <TextBlock Text="🔍" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                        </Button>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Section: Live Mode & Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0" Visibility="{Binding IsLiveMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border Background="{DynamicResource BgCard}" CornerRadius="6" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Padding="5">
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding LivePlayCommand}" Visibility="{Binding IsPaused, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{DynamicResource ModernBtn}" ToolTip="Resume Live Monitoring" Background="Transparent" Foreground="{DynamicResource SuccessColor}" Padding="5,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="▶" Margin="0,0,5,0"/>
                                    <TextBlock Text="RESUME" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding LivePauseCommand}" Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{DynamicResource ModernBtn}" ToolTip="Pause Live Monitoring" Background="Transparent" Foreground="{DynamicResource WarningColor}" Padding="5,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⏸" Margin="0,0,5,0"/>
                                    <TextBlock Text="PAUSE" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                            <Rectangle Width="1" Fill="{DynamicResource BorderColor}" Margin="5,2"/>
                            <Button Command="{Binding LiveClearCommand}" Style="{DynamicResource ModernBtn}" ToolTip="Clear &amp; Skip Old Logs" Background="Transparent" Foreground="{DynamicResource DangerColor}" Padding="5,0">
                                <TextBlock Text="⊘ CLEAR"/>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <Button Command="{Binding OpenJiraCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Open JIRA" Width="28" Height="28" Padding="0">
                    <Image Source="/Resources/jira.ico" Width="16" Height="16" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Button>
                <Button Command="{Binding OpenKibanaCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Open Kibana" Width="28" Height="28" Padding="0">
                    <Image Source="/Resources/kibana.ico" Width="16" Height="16" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Button>
                <Button Command="{Binding OpenOutlookCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Send Email" Width="28" Height="28" Padding="0">
                    <Image Source="/Resources/outlook.ico" Width="16" Height="16" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Button>

                <Rectangle Width="1" Fill="{DynamicResource BorderColor}" Margin="4,6"/>

                <Button Command="{Binding OpenHelpCommand}" Style="{DynamicResource ModernBtn}" Background="Transparent" ToolTip="Help (F1)" Width="28" Height="28" Padding="0" BorderThickness="0">
                    <TextBlock Text="❓" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
