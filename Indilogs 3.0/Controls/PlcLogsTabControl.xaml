<UserControl x:Class="IndiLogs_3._0.Controls.PlcLogsTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:IndiLogs_3._0.Controls"
             xmlns:views="clr-namespace:IndiLogs_3._0.Views"
             xmlns:converters="clr-namespace:IndiLogs_3._0.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <controls:PlcLogsGridControl x:Name="MainLogsGrid"
                                     DataContext="{Binding}"
                                     LogsSource="{Binding Logs}"/>
        <views:VisualTimelineView DataContext="{Binding VisualTimelineVM}" Visibility="{Binding DataContext.IsVisualMode, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
