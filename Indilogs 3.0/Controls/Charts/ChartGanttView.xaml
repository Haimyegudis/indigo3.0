<UserControl x:Class="IndiLogs_3._0.Controls.Charts.ChartGanttView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             Background="{DynamicResource BgDark}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource BgCard}" Padding="5,3">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="CHSTEP Gantt" FontWeight="SemiBold" FontSize="12"
                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                <ToggleButton x:Name="ExpandCollapseButton" Content="▼" Width="20" Height="20"
                              Margin="10,0,0,0"
                              Background="Transparent" BorderThickness="0"
                              Foreground="{DynamicResource TextPrimary}"
                              IsChecked="True" Click="ExpandCollapseButton_Click"
                              ToolTip="Expand/Collapse Gantt view"/>
            </StackPanel>
        </Border>

        <!-- Gantt Chart Canvas -->
        <Border x:Name="GanttContainer" Grid.Row="1" Background="{DynamicResource BgPanel}">
            <skia:SKElement x:Name="SkiaCanvas" PaintSurface="OnPaintSurface"/>
        </Border>
    </Grid>
</UserControl>
